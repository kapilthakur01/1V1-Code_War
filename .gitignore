# Dependencies
node_modules/
**/node_modules/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local
# NOTE: .env.production is intentionally committed — it only contains public URLs (no secrets).
# Secrets (JWT_SECRET, MONGODB_URI, etc.) live only in .env (local) and Render dashboard (production).

# Build outputs
frontend/dist/
frontend/build/

# Judge temp files
backend/temp/
temp/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Docker
*.dockerignore
".env" 
.env 
.env 
node_modules/ 
.env 
.cache/ 
